'use client';import{useEffect}from'react';import{motion,useMotionValue,useTransform,animate}from'framer-motion';export default function AnimatedNumber({value,decimals=0}:{value:number,decimals?:number}){const mv=useMotionValue(0);useEffect(()=>{const c=animate(mv,value,{duration:0.6,ease:'easeOut'});return c.stop;},[value]);const display=useTransform(mv,(v)=>v.toFixed(decimals));return <motion.span>{display}</motion.span>}